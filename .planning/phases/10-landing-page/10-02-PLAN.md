---
phase: 10-landing-page
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified: []
autonomous: false
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05
  - LAND-06

must_haves:
  truths:
    - "All 6 LAND requirements verified correct in a live browser"
    - "Authenticated users are redirected away from / without seeing landing content"
    - "Page is visually correct at 375px with no horizontal overflow"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Fully verified landing page"
  key_links:
    - from: "browser at /"
      to: "/dashboard"
      via: "server-side redirect for authenticated session"
      pattern: "redirect.*dashboard"
---

<objective>
Human verification of the complete landing page across all 6 LAND requirements.

Purpose: Confirm visual content, CTAs, 3-step flow, feature cards, auth redirect, and 375px mobile rendering are all correct before closing Phase 10.

Output: All LAND requirements sign-off; phase marked complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-landing-page/10-01-SUMMARY.md
@.planning/phases/10-landing-page/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and regression check</name>
  <files>none</files>
  <action>
    Run the full test suite and TypeScript check to confirm no regressions before asking the user to verify visually:
    1. `npx vitest run` — must show 77 tests passing (or more if any were added)
    2. `npx tsc --noEmit` — must show 0 errors
    3. `npm run build` — must complete without errors (catches server component issues)

    If any check fails, fix the issue in `src/app/page.tsx` before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>All three commands exit 0. No TypeScript errors in page.tsx. Build artifact is produced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify landing page end-to-end</name>
  <files>none</files>
  <action>Human verification of all 6 LAND requirements in a live browser — see how-to-verify instructions below.</action>
  <verify>
    <automated>MISSING — LAND-01 through LAND-06 require visual and interactive browser verification; Vitest node environment cannot render React components or simulate navigation.</automated>
  </verify>
  <done>All 6 LAND requirements confirmed: hero copy correct, 3 steps visible, 3 feature cards correct, both CTAs work, authenticated redirect fires, no overflow at 375px.</done>
  <what-built>
    Complete landing page (src/app/page.tsx) implementing:
    - LAND-05: authenticated-user redirect to /dashboard
    - LAND-01: hero with UP Cebu + UVEC Moodle + Google Classroom copy
    - LAND-02: "How it works" section with 3 numbered steps
    - LAND-03: 3 feature cards (drag-and-drop board, offline PWA, custom tasks)
    - LAND-04: primary CTA → /login, secondary CTA → GitHub README (new tab)
    - LAND-06: mobile-responsive layout at 375px
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open http://localhost:3000 in a browser.

    1. **LAND-01 — Hero copy** (unauthenticated):
       - Load `/` while logged out
       - Confirm the hero mentions "UVEC Moodle", "Google Classroom", and "UP Cebu" without scrolling
       - Expected: A headline + 1-2 sentences conveying the aggregation value for UP Cebu students

    2. **LAND-02 — How it works**:
       - Confirm a "How it works" section is visible with exactly 3 numbered steps
       - Step 1: Sign in with Google, Step 2: Connect UVEC Moodle, Step 3: Sync Google Classroom

    3. **LAND-03 — Feature cards**:
       - Confirm 3 feature cards with titles: "Drag-and-Drop Board", "Works Offline", "Custom Tasks"
       - Each card has an icon and short description

    4. **LAND-04 — CTAs**:
       - Click "Sign in with Google" primary button → confirm it navigates to /login (same tab)
       - Navigate back to /; click "Self-hosting guide" secondary button → confirm it opens a new tab to a GitHub URL

    5. **LAND-05 — Authenticated redirect**:
       - Sign in to the app (go through /login)
       - After sign-in, manually navigate to http://localhost:3000/
       - Confirm immediate redirect to /dashboard without seeing the landing page

    6. **LAND-06 — 375px mobile**:
       - Open DevTools; set viewport to 375px x 812px (iPhone SE)
       - Confirm no horizontal scrollbar appears
       - Confirm all sections stack single-column; text is readable and not clipped
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass. Describe any issues if not.</resume-signal>
</task>

</tasks>

<verification>
Phase 10 is complete when the human-verify checkpoint is approved with all 6 LAND requirements confirmed correct.
</verification>

<success_criteria>
1. `npx vitest run` — all existing tests green (no regressions)
2. `npx tsc --noEmit` — zero TypeScript errors
3. `npm run build` — build succeeds
4. All 6 LAND requirements confirmed by human verification:
   - LAND-01: Hero has UP Cebu + UVEC + GClassroom context above the fold
   - LAND-02: "How it works" shows 3 steps
   - LAND-03: Feature cards show drag-and-drop, offline PWA, custom tasks
   - LAND-04: Primary CTA to /login (same tab); secondary to GitHub (new tab)
   - LAND-05: Authenticated users redirected to /dashboard from /
   - LAND-06: No horizontal overflow at 375px
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page/10-02-SUMMARY.md` using the summary template.
</output>
